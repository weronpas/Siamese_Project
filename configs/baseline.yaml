# ==========================================
# SIAMESE NETWORK - BASELINE CONFIG
# ==========================================

paths:
  data_dir: "data/lfw"
  save_path: "best_siamese_model.pth"

hyperparameters:
  batch_size: 64
  learning_rate: 0.0001
  margin: 1.0

training:
  epochs: 100
  patience: 10